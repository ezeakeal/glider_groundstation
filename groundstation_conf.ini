[loggers]
keys=root

[handlers]
keys=stream,file

[formatters]
keys=formatter

[logger_root]
level=DEBUG
handlers=stream,file

[handler_stream]
class=StreamHandler
level=DEBUG
formatter=formatter
args=(sys.stderr,)

[handler_file]
class=handlers.TimedRotatingFileHandler
interval=midnight
backupCount=5
formatter=formatter
level=DEBUG
args=('/tmp/ground_station.log',)

[formatter_formatter]
format=%(asctime)s %(name)-12s %(levelname)-8s %(message)s

[output]
raw_dump=/tmp/raw.data
telemetry_dump=/tmp/telem.json

[led]
on=12
tx=13
rx=15

[radio]
device_path=/dev/ttyUSB1
address=0xFF
callsign=GroundStation
baud_rate=38400
enabled=True
glider_address=0xFF

[server]
# What port to start the webserver on
port=8000